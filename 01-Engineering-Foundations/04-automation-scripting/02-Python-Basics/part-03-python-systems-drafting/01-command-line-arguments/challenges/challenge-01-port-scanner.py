"""
Challenge: Port Scanner CLI
Scenario: You are building a network diagnostic tool. You need a CLI that takes 
a target host and a list of ports to scan.

TODO: Implement a CLI using `argparse`.
1. Define a required positional argument: 'host' (the target hostname).
2. Define an optional multi-value argument: '--ports' (list of integers).
3. Set the default for '--ports' to [80, 443].
4. Parse the arguments and print a message: "Scanning {host} on ports {ports}..."
"""
import argparse

def main():
    # --- START YOUR CODE HERE ---
    pass

if __name__ == "__main__":
    main()
