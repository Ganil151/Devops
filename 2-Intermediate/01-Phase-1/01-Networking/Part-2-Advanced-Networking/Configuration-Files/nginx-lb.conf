# Basic Nginx Load Balancer Configuration
http {
    upstream backend_servers {
        server 10.0.1.10:80;
        server 10.0.1.11:80;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://backend_servers;
        }
    }
}
