# Get-NetNat

## Purpose
Retrieves Network Address Translation (NAT) objects configured on the computer.

## Examples

### Get all NAT configurations
```powershell
Get-NetNat
```

### Get a specific NAT object by name
```powershell
Get-NetNat -Name "MyNATNetwork"
```
