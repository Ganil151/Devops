# EC2 Module

**Path:** `modules/ec2`

## Overview

Placeholder module for general-purpose EC2 instance management. This module is intended for deploying compute instances beyond the bootstrap jump host.

## Functions

- **Instance Creation**: Deploys EC2 instances with specified AMI and instance type
- **Networking**: Attaches instances to VPC and subnets with security groups
- **Key Pair Integration**: Associates SSH key pairs for instance access
- **Monitoring**: Supports CloudWatch detailed monitoring
- **User Data**: Supports custom initialization scripts
- **Tagging**: Applies consistent tags for resource management

## Inputs

| Name               | Description                           | Type         | Default |
| ------------------ | ------------------------------------- | ------------ | ------- |
| instance_type      | EC2 instance type (e.g., t3.micro)    | string       | ""      |
| ami_id             | AMI ID for the instance               | string       | ""      |
| key_pair_name      | SSH key pair name                     | string       | ""      |
| vpc_id             | VPC ID for instance placement         | string       | ""      |
| subnet_ids         | Subnet IDs for instance placement     | list(string) | []      |
| security_group_ids | Security group IDs for network access | list(string) | []      |
| user_data_base64   | Base64-encoded user data script       | string       | ""      |
| enable_monitoring  | Enable detailed CloudWatch monitoring | bool         | false   |

## Outputs

| Name                | Description                       |
| ------------------- | --------------------------------- |
| instance_id         | EC2 instance ID                   |
| instance_public_ip  | Public IP address (if applicable) |
| instance_private_ip | Private IP address                |
| instance_arn        | Instance ARN                      |

## Connections

- **Depends on**: VPC module (requires VPC/subnet IDs), Security Group module (requires SG IDs), Key Pair module (requires key name)
- **Used by**: Infrastructure management and workloads requiring EC2 compute
- **Purpose**: Provides additional compute instances beyond the jump host

## Module Structure

- `main.tf` - Resource definitions (placeholder - implementation needed)
- `variables.tf` - Input variables (placeholder - implementation needed)
- `output.tf` - Output values

## Usage

```hcl
module "ec2" {
  source = "../../modules/ec2"

  instance_type        = "t3.medium"
  ami_id               = "ami-0abcdef1234567890"
  vpc_id               = module.vpc.vpc_id
  subnet_ids           = module.vpc.private_subnet_id
  security_group_ids   = [module.security_group.sg_id]
}
```

## Implementation Status

This module currently contains placeholder code. Full implementation is needed to support:

- EC2 instance creation with configurable parameters
- Detailed monitoring configuration
- User data script execution
- Instance profile integration

---

_Generated by terraInfra_1.sh_
