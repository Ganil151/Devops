apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-petclinic-microservices.fullname" . }}
  labels:
    {{- include "spring-petclinic-microservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-petclinic-microservices.selectorLabels" . | nindent 4 }}
