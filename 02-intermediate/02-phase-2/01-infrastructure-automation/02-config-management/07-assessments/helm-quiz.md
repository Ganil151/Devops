# 🧠 Practice Quiz: Helm & Kubernetes Config

### 1. What is Helm often referred to as in the Kubernetes ecosystem?
- A) A code editor.
- B) A package manager.
- C) A runtime engine.
- D) A database.

### 2. What is the fundamental unit of deployment in Helm?
- A) A Container.
- B) A Chart.
- C) A Deployment.
- D) A manifest.

### 3. Which file contains the default configuration values for a Chart?
- A) config.yaml
- B) values.yaml
- C) settings.hcl
- D) Chart.yaml

### 4. What is the purpose of the `templates/` directory in a Helm Chart?
- A) To store images.
- B) To store YAML manifests that will be rendered with dynamic values.
- C) To store backup files.
- D) To store the README.

### 5. Which command is used to install a new Helm release?
- A) helm create
- B) helm install
- C) helm apply
- D) helm deploy

### 6. What is a 'Release' in Helm?
- A) A version of the software on GitHub.
- B) A specific instance of a chart running in a cluster.
- C) A type of docker image.
- D) A security patch.

### 7. Which command is used to see the history of a release and rollback if needed?
- A) helm log
- B) helm history
- C) helm status
- D) helm events

### 8. What is the purpose of the `Chart.yaml` file?
- A) It contains the passwords.
- B) It contains metadata about the chart (name, description, version).
- C) It contains the source code of the app.
- D) It's for documentation only.

### 9. Which tool is often used with Helm to manage multiple environments (Dev/Stage/Prod) declaratively?
- A) Ansible
- B) Terraform
- C) Helmfile
- D) Jenkins

### 10. How do you inject a value from `values.yaml` into a template?
- A) {{ .Values.keyname }}
- B) $keyname
- C) @keyname
- D) #keyname

---

## 🔑 Answer Key
1: B | 2: B | 3: B | 4: B | 5: B | 6: B | 7: B | 8: B | 9: C | 10: A
