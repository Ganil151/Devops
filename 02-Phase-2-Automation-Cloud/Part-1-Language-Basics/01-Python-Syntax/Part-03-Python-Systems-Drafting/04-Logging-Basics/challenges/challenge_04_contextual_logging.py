"""
Challenge: Contextual Logging
Scenario: You want to include a 'request_id' and 'user' in every log message 
to trace requests through your automation system.

TODO: 
1. Configure a logger with the format: 
   `[%(request_id)s] %(user)s: %(message)s`
2. Log messages by passing the `extra` dictionary:
   `logger.info("message", extra={"request_id": "...", "user": "..."})`
"""
import logging

# --- START YOUR CODE HERE ---

if __name__ == "__main__":
    # Test your implementation
    pass
