"""
Challenge: Subcommand Router
Scenario: You are building a multi-purpose tool. You want subcommands like `git` 
(e.g., `calc add 5 10` or `calc multiply 3 4`).

TODO: Implement a CLI with subcommands.
1. Use `add_subparsers()` to create a subcommand handler.
2. Add an 'add' subparser that takes two positional integers.
3. Add a 'multiply' subparser that takes two positional integers.
4. Execute the mathematical operation based on which subcommand was used.
"""
import argparse

def main():
    # --- START YOUR CODE HERE ---
    pass

if __name__ == "__main__":
    main()
